# Source assets

Optional: add images here and import them in components (e.g. `import img from './assets/photo.jpg'`). For the About portrait, use `public/assets/portrait.jpg` instead so it can be swapped without rebuilding; see `public/assets/README.md`.
